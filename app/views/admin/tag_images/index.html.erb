<div class="text-center">
  <div class="col-12 text-right">
    <%= button_to "Sync Images With Tags", apply_all_to_tags_admin_tag_images_path %>
  </div>
  <%= form_for TagImage.new, url: '/admin/tag_images' do |f| %>
    <div class="form-group">
      <%= f.file_field :image %>
    </div>
    <%= f.submit 'Upload Image', class: 'btn btn-outline-primary' %>
  <% end %>


  <% @tag_images.in_groups_of(3, false).each do |group| %>
    <div class="row">
      <% group.each do |tag_image| %>
        <div class="col-4" style="border: 1px solid black">
          <%= link_to admin_tag_image_path(tag_image) do %>
            <%= image_tag url_for(tag_image.image), class: 'img-fluid' %>
            <div class="col-12 text-center">
              <span class="badge bg-primary"><%= tag_image.tags.count %> Tags</span>
              <span class="badge bg-primary"><%= tag_image.lookup_patterns.count %> URL Patterns</span>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
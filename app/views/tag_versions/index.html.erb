<%= turbo_frame_tag "tag_#{@tag.uid}_tag_versions" do %>
  <%= render partial: 'tag_versions/tag_version_row', 
              collection: @tag_versions,
              as: :tag_version, 
              locals: { streamed: false } 
  %>
<% end %>

<%= paginate @tag_versions %>
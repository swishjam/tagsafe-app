<%= render 'show_page_header', page_title: "Performance Impact" %>

<%= render 'performance_audit', tag: @tag, tag_version: @tag_version, audit: @audit, previous_audit: @audit.audit_to_compare_with %>
<% if flash[:inline_errors] || local_assigns[:errors] %>
  <% (flash[:inline_errors] || local_assigns[:errors]).each do |err| %>
    <div class="error-container">
      <%= err %>
    </div>
  <% end %>
<% end %>
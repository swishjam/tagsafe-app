<div class="toast js-toast hide" style="min-width: 200px; position: absolute; top: 10px; right: 10px; z-index: 10000">
  <div class="toast-header">
    <i class="far fa-check-circle" style="color: green"></i>
    <strong class="mr-auto header"></strong>
    <button type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <div class="toast-body"></div>
</div>
<% if flash[:toast_errors] && flash[:toast_errors].any? %>
  <% flash[:toast_errors].each do |err| %>
    <%= render partial: 'partials/toast_message', locals: { 
      icon_class: 'fas fa-exclamation-triangle', 
      icon_color: 'red', 
      header: nil, 
      margin_top: '10px', 
      message: err 
    } %>
  <% end %>
<% end %>
<% if flash[:toast_messages] && flash[:toast_messages].any? %>
  <% flash[:toast_messages].each do |msg| %>
    <%= render partial: 'partials/toast_message', locals: { 
      icon_class: 'far fa-check-circle', 
      icon_color: 'green', 
      header: nil, 
      margin_top: '10px', 
      message: msg } %>
  <% end %>
<% end %>
import { config } from './config';
import { logData } from './log';
import { rt } from './metrics';
export var initResourceTiming = function (performanceEntries) {
    performanceEntries.forEach(function (entry) {
        if (config.isResourceTiming) {
            logData('resourceTiming', entry);
        }
        if (entry.decodedBodySize && entry.initiatorType) {
            var bodySize = entry.decodedBodySize / 1000;
            rt.value[entry.initiatorType] += bodySize;
            rt.value.total += bodySize;
        }
    });
};
//# sourceMappingURL=resourceTiming.js.map
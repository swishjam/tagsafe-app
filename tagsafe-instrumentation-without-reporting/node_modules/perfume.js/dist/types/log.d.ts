import { Metric } from './types';
export declare const logData: (measureName: string, metric: any, attribution?: object) => void;
/**
 * Dispatches the metric duration into internal logs
 * and the external time tracking service.
 */
export declare const logMetric: ({ attribution, name, rating, value }: Metric) => void;

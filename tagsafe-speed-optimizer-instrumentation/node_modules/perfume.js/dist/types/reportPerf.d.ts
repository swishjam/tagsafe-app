import { IVitalsScore } from './types';
/**
 * Sends the User timing measure to analyticsTracker
 */
export declare const reportPerf: (measureName: string, data: any, rating: IVitalsScore, attribution: object) => void;

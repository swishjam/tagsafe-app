{
  "name": "lambda-puppeteer",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "deploy-prod": "serverless deploy --stage production",
    "deploy-staging": "serverless deploy --stage staging --profile staging",
    "deploy-dev": "serverless deploy --stage collin-dev",
    "locally": "serverless invoke local --function find-third-party-tags --path _localInvokeData.json --stage local"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "dotenv": "^10.0.0",
    "node-fetch": "^2.0.0",
    "puppeteer-core": "^10.4.0"
  },
  "devDependencies": {
    "puppeteer": "^10.1.0",
    "serverless": "^2.57.0",
    "serverless-offline": "^8.1.0"
  }
}
